<%= render "shared/page_title", content: "Classes" %>
<%= render "shared/page_heading", content: "Enroll in a Class:" %>
<%= render "shared/flash_notice" %>


<% @groups.each do |g| %>
    <%= link_to "Enroll: #{g.name } -#{g.teacher.last_name}", group_enrollments_path(g), method: 'post' %>
<% end %>