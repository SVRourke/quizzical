<section class="quiz-block completed">
    <div class="block-heading">
        <h3><%= q.name%></h3>
    </div>
    <p>Grade: <%= q.user_grade(current_user) %></p>
    <%= link_to "+Note", notes_quiz_path(q.id) %>
</section>