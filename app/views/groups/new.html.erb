<h1>Create A Group:</h1>

<%= form_for @group do |f| %>
    <%= render 'shared/validation_errors', :record => @group %>
    <%= f.label "Group Name: ", :name%><br>
    <%= f.text_field :name %><br>
    <%= f.submit "Create Group" %>
<% end %>