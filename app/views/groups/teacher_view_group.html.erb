<%= render "shared/page_title", content: @group.name %>

<% content_for :new_quiz_link do %>
    <%= link_to "Create A Quiz", new_group_quiz_path(@group) %>
<% end %>

<!-- move to nav bar -->

<h2>Quizzes</h2>
<div class="block-display">
    <% @group.quizzes.each do |q| %>
        <%= link_to group_quiz_path(@group, q), class: "block-link" do %>
            <section class="class-block">
                <div class="block-heading">
                    <h4><%= q.name %></h4>
                </div>
                <p><%= student_results_count(q) %></p>
            </section>
        <% end %>
    <% end %>
</div>