<%= link_to(group, class: "quiz-link") do %>
    <section class="quiz-block group">

        <div class="block-heading">
            <h3><%= group.name %></h3>
            <p>Teacher: <%= group.teacher.full_name %></p>
        </div>

        <p><%= quizzes_due(group) %> Quizzes Due</p>

    </section>
<% end %>