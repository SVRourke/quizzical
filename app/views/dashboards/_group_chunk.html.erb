<h4><%= group.name %></h4>
<%= link_to "View", group_path(group) %>
<p>Teacher: <%= group.teacher.full_name %></p>
<p>
    Quizzes Total: <%= group.quizzes.ready.count %>, 
    Due: <%= group.quizzes.ready.count - group.results.where(user: current_user).count %>, 
    Completed: <%= group.quizzes.filter {|q| q.completed_by(current_user)}.count %>
</p>
